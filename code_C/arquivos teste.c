#include <stdio.h>
#include <stdlib.h>

int main()
{
	FILE *arq;
		
	arq = fopen("teste.txt", "a");
	
	fprintf(FILE *arq, %c, arq);
	

	if((arq = fopen("teste.txt", "w")) == NULL)
	{
		printF("Não foi possivel criar o arquivo\n");
		exist(1);
	}
	
	int fclose (FILE *arq);
	
	return 0;
}
